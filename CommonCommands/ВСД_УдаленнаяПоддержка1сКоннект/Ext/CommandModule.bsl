// GRACE:MODULE_START name="КомандыВСД"
// @grace-module: КомандыВСД
// @grace-purpose: Команда запуска удалённой поддержки 1С:Коннект — получает URL и открывает приложение, ошибки поглощаются
// @grace-depends: ЯдроВСД, ДокументооборотВСД, ПроизводствоВСД, ИнтеграцияВетИС_API
// @grace-version: 1.0
// @grace-contract-ref: docs/Contracts.xml -> module="КомандыВСД"
// GRACE:DEPENDENCY target="кб99_ВСД"

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)

	// LLM_NOTE: Попытка без обработки — намеренно. Запуск 1С:Коннект вспомогательная функция,
	// недоступность приложения не должна прерывать работу пользователя.
	Попытка
	    АдресЗагрузкиПриложения = ВетИС_ЯдроСервер.ПолучитьПутьКПриложениюУдаленнойПоддержки();
		Оповещение = Новый ОписаниеОповещения("ЗапускПриложения", ЭтотОбъект);
		НачатьЗапускПриложения(Оповещение, АдресЗагрузкиПриложения._1cConnect,, Истина);
	Исключение КонецПопытки;

КонецПроцедуры

// @grace-contract: ЗапускПриложения
//   Вход: КодВозврата (Число) — код возврата процесса (не используется)
//   Вход: ДополнительныйПараметр (Произвольный) — передаётся платформой, не используется
//   Выход: нет (stub-callback)
//   Инвариант: обязательный callback для НачатьЗапускПриложения
//   GRACE-лог: нет
// GRACE:CONTRACT function="ЗапускПриложения"
&НаКлиенте
Процедура ЗапускПриложения(КодВозврата, ДополнительныйПараметр) Экспорт

    // LLM_NOTE: Заглушка — НачатьЗапускПриложения требует ОписаниеОповещения. Результат не обрабатывается.
    а = 1;

КонецПроцедуры
// GRACE:MODULE_END name="КомандыВСД"
